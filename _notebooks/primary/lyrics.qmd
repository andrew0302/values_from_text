---
title: "lyrics"
---

```{r}
library("here")
library("tidyverse")
library("cowplot")
```

```{r}
lyrics_df <- readRDS(here("_data", "_primary_data", "public_lyrics_dt.RDS"))
```

```{r}
count_df <- lyrics_df %>% #filter(Keep == "o") %>%
  group_by(artist_name) %>% count() %>% group_by(n) %>% count()
 
# count_df %>%
#   ggplot(aes(x=n)) +
#   geom_histogram() +
#   theme_minimal()
```

```{r}
count_df <- lyrics_df %>% filter(Keep == "o") %>%
  group_by(artist_name) %>% count()
```

